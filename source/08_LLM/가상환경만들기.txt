※ 가상환경 만들기(llm)

(가상환경 목록 확인) conda env list (conda info --envs)

(가상환경생성:-n:--name을 의미) conda create -n llm python=3.10

(생성한 가상환경으로 전환) conda activate llm

(pip 최신버전으로 업그레이드) python -m pip install --upgrade pip

(가상환경에서 jpykernel 설치) conda install ipykernel

(가상환경을 jupyter에서 선택가능한 커널로 등록)
python -m ipykernel install --user --name llm --display-name "llm(ipykernel)"

(가상환경 등록 확인)  jupyter kernelspec list

(가상환경을 jupyter 커널 목록에서 삭제 하기) jupyter kernelspec remove 가상환경이름(ml-dl-nlp)
(가상환경 지우기) conda env remove --name 가상환경이름

수업중 사용할 라이브러리
pip install langchain-ollama # ollama(1.0.0)
pip install langchain-openai # gpt 쓰기 위함(1.1.0)
pip install python-dotenv # 1.2.1
pip install python-decouple
pip install langchain-anthropic # claude 모델(1.2.0)

OpenAI Model을 사용하기 위한 라이브러리
pip install openai
pip install matplotlib

pip install streamlit # 웹 서비스 데모 (1.52.1)
pip install sounddevice scipy # sounddevice (0.5.3), scipy(1.15.3) 마이크 입력 녹음하여 전송하기
(가상환경을 jupyter 커널 목록에서 삭제 하기) jupyter kernelspec remove 가상환경이름(ml-dl-nlp)
(가상환경 지우기) conda env remove --name 가상환경이름